# Neon connection string for a dedicated bond database or branch.
# Leave this unset for the local JSON portfolio file fallback.
BONDS_DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Optional. Set to "file" to force the private portfolio to use a local JSON file
# even when BONDS_DATABASE_URL is configured. Defaults to .data/bonds-portfolio.json.
BONDS_PORTFOLIO_DATABASE=file
BONDS_FILE_DATABASE_NAME=bonds-portfolio.json

# Optional. When omitted, the temporary owner account built into the app is used.
BONDS_ADMIN_EMAIL=you@example.com

# Optional. When omitted, the temporary owner password built into the app is used.
# Generate with: node scripts/hash-bonds-password.mjs 'a-long-unique-password'
BONDS_ADMIN_PASSWORD_HASH=salt:hexadecimal-scrypt-hash

# Optional during local development. Generate before making the site public:
# openssl rand -base64 48
BONDS_SESSION_SECRET=replace-with-at-least-32-random-characters

# Optional. Protects /api/bonds/rse/snapshot when called by a scheduler.
BONDS_MARKET_SNAPSHOT_SECRET=replace-with-another-long-random-secret
